<%= form_with(model: task, local: true) do |ans| %>

    <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  　<% end %>
  
    <%= ans.label :content, "新規の予定" %>
    <%= ans.text_field :content %>
    
    <%= ans.label :status, "ステータス" %>
    <%= ans.text_field :status %>
    
    <%= ans.submit "追加" %>
<% end %>